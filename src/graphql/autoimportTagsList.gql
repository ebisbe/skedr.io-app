 query autoimportTagList($userId: ID!) {
  autoimportTagList(userId: $userId) {
    groupId
    tags
    group{
      title
      icon
      poolCount
      photoLimitOptOut
      members
      throttleMode
      throttleCount
      throttleRemaining
    }
  }
}
