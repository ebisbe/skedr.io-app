query autoimportTagList {
  groupTagsList {
    groupId
    tags
    group {
      id
      groupId
      title
      icon
      poolCount
      photoLimitOptOut
      members
      isMember
      throttleMode
      throttleCount
      throttleRemaining
    }
  }
}
