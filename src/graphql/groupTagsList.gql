query autoimportTagList {
  groupTagsList {
    id
    groupId
    tags
    group {
      groupId
      title
      icon
      poolCount
      photoLimitOptOut
      members
      isMember
      throttleMode
      throttleCount
      throttleRemaining
    }
  }
}
