query autoimportTagList {
  groupTagsList {
    id
    groupId
    tags
    group {
      id
      groupId
      title
      icon
      poolCount
      photoLimitOptOut
      members
      isMember
      throttleMode
      throttleCount
      throttleRemaining
    }
  }
}
