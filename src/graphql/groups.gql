query groups($userId: ID!, $offset: Int) {
  userGroups(userId: $userId, count: 100, offset: $offset) {
    title
    groupId
    icon
    poolCount
    members
    throttleMode
    throttleCount
    throttleRemaining
  }
}
