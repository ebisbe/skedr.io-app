mutation updateGroupTagsList($groupId: String!, $tags: [String!]) {
  updateGroupTagsList(input: { groupId: $groupId, tags: $tags }) {
    groupId
    tags
    group {
      title
      icon
      poolCount
      photoLimitOptOut
      members
      throttleMode
      throttleCount
      throttleRemaining
    }
  }
}
