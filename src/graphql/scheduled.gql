query scheduledPhotos($scheduledAt: Int!) {
  scheduledPhotos(scheduledAt: $scheduledAt) {
    userId
    groupId
    photoId
    scheduledAt
    photo {
      title
      server
      farm
    }
    group {
      title
    }
  }
}
